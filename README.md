# canopy-iiif.github.io

Canopy IIIF moved from the `canopy-iiif` org to [`nulib-ds`](https://github.com/nulib-ds/canopy). This repository now hosts a single static redirect page so `https://canopy-iiif.github.io/` sends visitors to **https://nulib-ds.github.io/canopy/**.

This is no longer managed by release automation — it's a plain static `index.html`, edited directly.
